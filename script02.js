// baseado no comando que for informado,
//mostra a lampada ligada ou desligada

const comando =prompt(Informe o comando para lampada (ligar /desliga))
const elementoImagem  document.createelement("img")

switch (comando . toLowerCase  ()) {
    case "liga"
    elementoImagem.src=."/asserts/on.png"
    break;
    case desligado
}